# macOS
.DS_Store

# ── Showcase examples (repo root) ────────────────────────────
# Track JSON only; HTML is built locally for preview.
examples/*.html

# Dev/test fixtures — not part of the public showcase
examples/preview-*
examples/frontend-*
examples/overflow-test.*
examples/path-test.*
examples/sample-standalone.*
examples/*.pdf

# ── Agent usage examples (skill package) ─────────────────────
resume-writer/examples/*.html

# ── Node ────────────────────────────────────────────────────
node_modules/

# ── Legacy / LaTeX auxiliary files ──────────────────────────
*.aux
*.bbl
*.bcf
*.blg
*.fdb_latexmk
*.fls
*.lof
*.log
*.lot
*.out
*.run.xml
*.toc
*.xdv
*.synctex.gz
*.pdf

# ── Temporary files ──────────────────────────────────────────
*.tmp
*.bak
*~
